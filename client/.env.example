# API base URL (used when running frontend)
# Local dev: use http://localhost:5000/api (create .env.local with this)
# Production / deployed: leave unset or set to your deployed backend URL
# NEXT_PUBLIC_API_URL=http://localhost:5000/api
